# Collector Completion Pass V1

September 11, 2026. Founder direction: finish end to end without micro-approvals.

Continue local implementation, bounded tests and preservation without stopping at
each internal checkpoint. Keep the approved design, original sample, protected
read-only preview and production unchanged. No push, production switch, mobile
release or production write is implied.

## Positive Browser Pricing

`collector-pricing-replay.mjs --browser` restores the existing local schema/public
reference into a new named fixture database. Only this database receives the
explicit synthetic identity/source/mapping/qualification/publication and four
owned-copy fixtures. Normal rollback-only replay remains the default.

GoTrue and PostgREST containers reuse installed local images, with their database
URLs pinned to that new database. No source users/sessions copied. Two synthetic
users are created through GoTrue. The loopback gateway forwards real auth/RPC
requests, not mocked responses. No email, external auth or worker activation.

API 54361 and website 3168 must be unused. Explicit fixture mode refuses all other
database endpoints; normal preview refuses the fixture endpoint. Separate Next
build directory protects the running preview. A persistent visible banner states
that prices are synthetic, not market data. Secret files remain outside Git with
operator/SYSTEM-only ACL. All created services stop on completion/failure; database
and audit artifacts remain. No cleanup of prior fixtures or original sample.

Require real browser price, quantity-weighted totals, unpriced/graded exclusions,
outsider isolation, desktop/mobile screenshots, source readback, and passing tests.
This is browser integration proof, not live pricing evidence or production release.

## Other Work

Finish recoverable local bugs, Binder authority replay, least-privilege cameo
projection and full signed-in smoke coverage. Record external authority/data gaps
honestly rather than fabricate facts or silently expand production privileges.
Update one final checkpoint with completed work and genuine external dependencies.

## Fixture Authority And Evidence

The browser replay may apply the Set Binder and cameo candidate SQL only to its
new named database. Copy the local auth migration ledger, never users or sessions.
One explicitly synthetic Set manifest exercises creation, contribution, progress,
withdrawal and outsider denial. It is not a reviewed real catalog denominator.
Set options must come from the bounded authorized-manifest RPC, not every set row.

The fixture gateway serves one hash-verified existing local canonical image, read
without changing source Storage. Other Storage paths are not proxied. Capture real
decoded images and desktop/mobile layouts, not loading skeletons. After the test,
verify its labeled auth/REST containers are stopped and retain the test database.

Cameo SQL tests are rollback-only. A curated source match is not visual authority.
Only active image/role-confirmed evidence whose source and image still match can
enter the safe projection. Representation/depiction needs a separate host. Raw
notes and unconfirmed associations are never a public fallback. Original sample
and production schema are untouched; optional missing RPCs fail closed in clients.

## Completion Boundary

The local functional candidate and its reproducible tests can be completed without
another internal approval. Hosted writable staging, real complete Set manifests,
real cameo confirmations, positive non-Pokemon catalog coverage and native share/
print delivery remain separate evidence dependencies. Do not substitute the remote
restore-drill project for staging, deploy to production, or call synthetic pricing
proof a live-market release. Record these limits once in the consolidated report.
