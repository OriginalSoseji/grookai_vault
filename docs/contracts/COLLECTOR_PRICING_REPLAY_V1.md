# Collector Pricing Replay V1

September 11, 2026. Local verification only; no production authority.

Continue authenticated collector staging without changing its approved design.
Create a uniquely named local replay database from the existing loopback schema
and bounded public catalog reference sample. Never reset or write to the source
sample, production, recovery project, or protected Vercel deployment.

Use one explicitly synthetic source product/price observation, mapped to the copied
Blastoise ex parent and exact Holo printing. No invented canonical card or printing.
The sample lacks `card_print_identity` records. First prove qualification rejects
that missing authority; then insert one explicitly synthetic identity fixture for
the unchanged copied parent inside the same rollback-only transaction. This is
test scaffolding, not a recovered/approved canonical identity or production evidence.
All price/source/ownership fixtures and the test publication pointer exist inside
one transaction that must roll back. The replay database has no app/API route,
scheduled jobs, network extension, copied users, or copied secrets.

Exercise the actual assignment function, candidate view, pure qualification policy,
activation guard, authenticated pricing RPC and web pricing/total functions. Do not
start a worker or claim the fixture's staged phase records prove worker execution.
Fixture price 12.34 is test data, never an actual TCGPlayer price. Keep synthetic
labels in run keys, source payloads, provenance and every resulting artifact.

Require two exact raw copies to total 24.68; unresolved and graded copies stay
unpriced. Test anonymous denial, cross-account ownership isolation, request bounds,
stale source and wrong-currency policy rejection, and activation reconciliation.
Export the real SQL read response plus web reader/total results for traceability.
Read back zero fixture rows after rollback and verify original sample unchanged.

This is SQL-to-web-function integration, not HTTP/browser, live ingestion, scheduled
worker or production publication certification. No customer-facing test price,
schema migration, deployment, Storage write, or pricing-policy relaxation.
