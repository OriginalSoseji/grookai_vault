# MEE Public Price Bridge V1

The Market Evidence Engine now has a narrow authenticated app-facing bridge.

The bridge intentionally starts small: only clean, internally approved ordinary raw-single active-listing estimates. Special lanes and slabs remain held for later policy work.

This is the first point where MEE-derived values can appear in the app, and the copy must remain honest that these are active listing estimates, not sold comps or guaranteed market truth.
