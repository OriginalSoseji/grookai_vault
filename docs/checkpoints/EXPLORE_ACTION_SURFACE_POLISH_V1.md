# EXPLORE ACTION SURFACE POLISH V1

## Purpose
Refine the Explore drawer so ownership state becomes actionable, add/remove behavior feels intelligent, and the surface reads like a premium collector interaction layer.

## Current Problems
- `In your vault` is informative but not actionable
- owned state should lead directly to exact-copy truth
- `Remove from Vault` is missing from the Explore drawer
- add confirmation is not surfacing reliably before navigation
- the drawer still reads slightly like a generic options sheet instead of a collector-grade action surface

## Locked Decisions
- keep Compare
- keep Compare secondary
- keep tiles image-first with no overlays
- keep drawer as the Explore action center
- replace `In your vault` with `View your copy`
