import '../widgets/badges.dart';  // add at top with your imports

// Inside your ListTile / card builder:
subtitle: Column(
  crossAxisAlignment: CrossAxisAlignment.start,
  children: [
    Text('${row['set_code'] ?? ''} • ${row['number'] ?? ''}'),
    const SizedBox(height: 6),
    Wrap(children: buildBadges(row)),
  ],
),
