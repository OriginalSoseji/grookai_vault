import 'package:flutter/material.dart';

class WallProfilePage extends StatelessWidget {
  const WallProfilePage({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Wall Profile')),
      body: const Center(child: Text('Profile & posts (MVP placeholder)')),
    );
  }
}

