import 'package:flutter/material.dart';

/// Thunder Aesthetic core tokens: subtle, premium accents.
class Thunder {
  static const Color accent = Color(0xFFFFD54F); // warm electric
  static const Color accentAura = Color(0xFFFFE082); // outer halo
  static const Color electricBlue = Color(0xFF00E5FF); // optional cool accent
  static const Color base = Color(0xFF0F1115); // canvas
  static const Color surface = Color(0xFF161A20); // cards
  static const Color surfaceAlt = Color(0xFF1B2028); // alt cards
  static const Color onSurface = Color(0xFFECEFF4);
  static const Color muted = Color(0xFF9AA4B2);
  static const Color divider = Color(0x1AFFFFFF);
}
