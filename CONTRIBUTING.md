Contributing
------------

- Read docs/WORKFLOW_RULES.md first.
- Local dev runs with `--dart-define=GV_ENV=local`. Use the VS Code task “Grookai: Ensure + Run (LOCAL)”.
- Staging sanity: see docs/AUTH_STAGING_CHECK.md.
- Do not commit secrets. `.env` is ignored; suffixed env files contain placeholders.

