name: Guard: No Legacy Keys
on:
  pull_request:
  push:
    branches: [ main ]
jobs:
  scan:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run legacy key guard
        shell: pwsh
        run: .\scripts\guard_no_legacy_keys.ps1

