name: Auto-Align Import-Prices (LEGACY DISABLED)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy pricing auto-deploy is disabled; use the retired-pricing contract workflow."
